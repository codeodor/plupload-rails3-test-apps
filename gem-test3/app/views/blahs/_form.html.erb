<%= form_for(@blah) do |f| %>
  <% if @blah.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blah.errors.count, "error") %> prohibited this blah from being saved:</h2>

      <ul>
      <% @blah.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= plupload(@blah, :payload, options={:plupload_container=>'uploader'}) %>
  <div id="uploader" name="uploader" style="width: 100%;"></div>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :payload %><br />
    <%= f.text_field :payload %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
